    steps:
      - name: Run Crawler
        env:
          # এখানে বাম পাশের নামগুলো পাইথন কোডে os.environ.get হিসেবে কাজ করবে
          PROJECT_URL: ${{ secrets.PROJECT_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python crawler.py
